# Framework Design

Internal design doc for team-fw (restricted).

See the `Widget` class and the `render_widget` function. The `retry_backoff`
setting controls how aggressively the client retries failed requests.
